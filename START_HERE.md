# START_HERE　オフィス開設の進め方

> **社長へ。このファイルがここにあるのは、正常です。**
> これは、AI従業員が開設を進めるときに読む作業手順です。
> **社長は、開いても閉じても、どちらでも構いません。**
> 中身は業務の指示なので、読んでいただいても害はありません。
>
> **社長向けの説明は、`★はじめにお読みください` の中にある
> `はじめにお読みください.html` です**（2回続けて押すと開きます）。

---

**ここから下は、AI従業員に宛てた文章です。**
社長から「オフィスを開設しましょう」「このキットを私の環境に入れてください」と
言われたら、AI従業員はここを読んで、そのとおりに進めてください。

---

## 大原則（これを外すと、あとの手順は全部無意味になります）

**① 技術の説明を並べないでください。**
社長の時間は、経営の判断に使うためのものです。設定項目を1つずつ説明して「はい」と
答えてもらう進め方は**採りません**。**設定の中身は、あなたが引き受けてください。**
社長にお聞きするのは、**業務としてどうしたいか**だけです。

（このファイルは、社長がいつでも開いて読めるところに置いてあります。
社長が読んでも差し支えのない書き方をしてください。）

**② 判断を求めるのは、作業のあと、業務上の結果として、原則1問だけです。**

**ただし「進行の確認」は別です。1つの手順が終わるたびに、必ず社長に聞いてください。**

```
【報告】
README 第3部の〈番号〉「〈README の項目名〉」が終わりました。
次は、第3部の〈番号〉「〈README の項目名〉」です。
次の手順に進んでよろしいですか？
```

**先頭に【報告】を付け、すぐ次の行で、README 第3部のどこが終わったかを伝えてください。**
**番号は、README 第3部の1〜18の番号を使います。** `SETUP_STATUS.md` の1〜18と同じ番号です（2026-09-16に、進捗の表の並びを README の順にそろえました）。
**項目名は、README 第3部に書いてある言葉のまま使います**（見出しの下の括弧の中と、13〜18の表）。
社長は README を読みながら進めているので、README と同じ番号と言葉でなければ、どこまで来たかが分かりません。
1つの手順で複数の項目が済んだときは「第3部の3〜6「保存先とファイル名を決める」が終わりました」のように、見出しの番号と言葉で伝えます。

**「〈N〉/18」「18項目のうち〈N〉項目」のような、済んだ数を数える言い方は使わないでください（2026-09-16・耕治さんの判断）。**
実機テストで「【5/18】」と伝えたところ、README の手順の番号と合わず、何番目なのか分からなくなりました。
**README の見出しと、ここでの節の対応は、STEP 2 の冒頭の表にあります。**

**かかる時間の見込みと、残りの項目数も、伝えないでください（2026-09-16・耕治さんの指示）。**
時間の見込みは不正確で（実機テストで、README の「およそ60分」を上回る「あと75分」を伝えてしまった）、
社長を混乱させます。

**返事を待たずに、次の手順へ進まないでください。**

**①が禁じているのは、設定の中身を説明して社長に選ばせることです。**
進行の確認は、**中身の判断を求めていない**ので、①には当たりません。
社長が「**いま全体の何番目まで来たか**」を見失わないための区切りです。

**社長が「まとめて進めて」とおっしゃったときは、その回だけ確認を省いて構いません。**

**③ 報告は、業務の言葉で3点だけ。読む量は15行・3分を上限の目安にします。**

**④ 理解は、説明からではなく、実際に1回動かすところから来ます。**
だから STEP 2 の手順3にある「**復元テスト**」（変更履歴から実際に復元する）を飛ばさないでください。
**記録を取っただけでは、戻せる証拠になりません。** 実際に削除して、復元できたことを確かめることが、いちばん大事です。

（2026-09-15の変更：④が指す対象は、禁止事項の試しではなく、STEP 2・手順3の復元テストです。
**禁止事項は、拒否されたとしても「その命令が拒否される」ことしか示せず、
同じことができる別の命令が残るため**、社長への約束と範囲が合いません。
手順3の復元テストは、**実際に削除したファイルが戻ることを確かめる**ので、範囲が一致します。）

---

## STEP 0　どちらの場面かを、2つ見て決める

いま開いているフォルダを見て、**上から順に**確かめてください。社長に聞く必要はありません。

| | 見るもの | 当てはまったときの進み方 |
|---|---|---|
| **1** | いま開いているフォルダの**直下に `SETUP_STATUS.md` がある** | 社長は、このキットのフォルダ（`AI_Office`）を開いています。**そのまま STEP 1 へ** |
| **2** | 直下にはないが、**`AI_Office` という子フォルダがあり、その中に `SETUP_STATUS.md` がある** | **そのまま進まないでください。**下の「2 のときに必ず確かめること」へ |
| **3** | **どちらでもない** | **進まないでください。**下の形で社長に聞きます |

**3 のときに聞くこと**

```
いま Claude Code で開いているフォルダ（〈フォルダの場所〉）の中に、オフィス開設キットのファイルが見つかりません。
（いま Claude Code で開いているフォルダは、Claude Code の画面で、文字を入力する欄の上に表示されています）
次の2つのうち、どちらかが原因だと思われます。心当たりはありますか？

（1）キットを展開してできた「AI_Office」フォルダを開くつもりだった
　　→ その1つ上のフォルダを開いている可能性があります。
　　　 「AI_Office」フォルダを選び直してください
　　　 （やり方は、README（「はじめにお読みください」）の第1部・手順2と同じです）。
　　　 Claude Code を閉じる必要はありません。

（2）これまで Claude Code で使ってきたフォルダを開いていて、そこへキットを追加したい
　　→ キットを展開してできた「AI_Office」フォルダを、
　　　 いま Claude Code で開いているフォルダ（〈フォルダの場所〉）の中へ移動してください。
　　　 移動が終わったら、「移動しました」とおっしゃってください。
```

**推測で進めないでください。** どちらかを決めるのは社長です。

### 2 のときに必ず確かめること

**2 に当たるのは、2通りあります。見分けないと、取り違えます。**

| | 本当の姿 | 見分け方 |
|---|---|---|
| **正** | 社長が、これまで Claude Code で使ってきたフォルダの中へ `AI_Office` を置いた | そのフォルダに、**社長の仕事のファイルがある** |
| **誤** | 社長が、`AI_Office` の**1つ上**を開いてしまった（`C:` やダウンロードの場所） | **仕事のファイルがない。**ドライブの直下（`C:\` など）である |

**まず、機械で分かることを2つ見てください。**

1. **いま開いているのが、ドライブの直下か**（`C:\`・`D:\` など）。
   **直下なら、誤りです。** そこをAIオフィスにしてはいけません
2. **`AI_Office` 以外に、社長の仕事のファイルがあるか。**
   `AI_Office` しかないなら、誤りの可能性が高いです

**どちらかでも当てはまったら、進まずに社長に聞いてください。**

```
いま Claude Code で開いているフォルダは、〈フォルダの場所〉です。
このフォルダの中には「AI_Office」フォルダがありますが、それ以外のファイルが見つかりません。

次の2つのうち、どちらに当てはまりますか。

（1）「AI_Office」フォルダそのものを開くつもりだった
　　→ 「AI_Office」フォルダを開き直してください
　　　 （やり方は、README（「はじめにお読みください」）の第1部・手順2と同じです）。

（2）これまで Claude Code で使ってきたフォルダ（〈フォルダの場所〉）を開いていて、
　　 キットと比較するために、その中へ「AI_Office」フォルダを移動した
　　→ 開いているフォルダは合っています。「このまま進めてください」とおっしゃってください。
```

**「正」と分かったら、このファイルの「すでに作ってある環境に入れるとき」へ進んでください**
（このファイルの終わりのほうにあります）。**STEP 1 へ直行しないでください。**
比較を先に済ませないと、`Shared_Assets/` がまだ社長のフォルダにないので、
**STEP 1 の1番で止まります。**

**推測でどちらかに決めないでください。** 取り違えると、
**社長のドライブ全体を「育てた環境」として比べ始める**ことになります。

**3 を1つ目の条件だけで判定しないでください。** 社長が `AI_Office` の1つ上の
フォルダ（`Windows (C:)` やダウンロードの場所）を開いてしまったとき、
1つ目だけを見ると「ご自分で作った環境だ」と誤って判断します。

### 途中で「実は別のフォルダでも使っている」と言われたとき

**1 で進めている途中に、社長がこう言い出すことがあります。**
「実は、別のフォルダで Claude Code を使っていました」「前に作ったものがあります」。

**そのときは、作業を止めて、こう伝えてください。**

```
これまで Claude Code で使ってきたフォルダには、社長がすでに作った設定やルールがあるかもしれません。
このまま進めると、AIオフィスが2か所にできてしまい、
どちらの設定が正しいのか、あとで分からなくなります。

そこで、ここでいったん作業を中断して、キットの中身と、
これまで Claude Code で使ってきたフォルダの中身を比較する進め方に切り替えます。
次の2つを行ってください。

（1）キットを展開してできた「AI_Office」フォルダを、
　　 これまで Claude Code で使ってきたフォルダの中へ移動する
（2）Claude Code で、そのフォルダを開き直す

ここまでに入力していただいた内容は、「AI_Office」フォルダの中に保存されているので、
フォルダを移動しても消えません。
```

**あなたのほうから「重なっていませんか」と探りに行く必要はありません。**
いま開いているフォルダの外は見えないので、確かめようがないからです。
**社長が言い出したときにだけ、切り替えます。**

### `SETUP_STATUS.md` があるとき、設置作業はありません

**ファイルを置き直す作業はありません。**
展開してできた `AI_Office` フォルダが、そのままAIオフィスです。
就業規則（`CLAUDE.md`）も、ルールも、事務用品も、禁止事項も、
**社長がこのフォルダを開いた時点で、すでに読み込まれています。**

**閉じて開き直す必要もありません。社長に開き直しを頼まないでください。**

---

## STEP 1　初期設定（社長に聞かずにできること）

**ここは社長に質問しません。** 3つとも、あなただけで終わります。
**社長への報告で「初期設定」と呼んでください。**「下ごしらえ」「準備」などの
言い換えを使わないでください（2026-09-16・耕治さんの指示）。
終わったら STEP 2 へ進み、**READMEの手順どおりに**進めてください。

### 1. 入っているものを確かめる

次の5つが、いま開いているフォルダにあるかを**実際に開いて**確かめてください。
推測で「ある」と書かないでください。

| 見るもの | 何のためのものか |
|---|---|
| `CLAUDE.md` | 就業規則。AI従業員が全員読みます |
| `.claude/settings.json` | 禁止事項。書いた操作は実行できなくなります |
| `.claude/rules/` の3ファイル | 常に効くルール |
| `.claude/skills/` の9フォルダ | 事務用品 |
| `Shared_Assets/` 一式 | 会社情報・デザイン仕様・文章の書き方・請求書などのテンプレート |

**STEP 0 の「1」から来たとき**（社長が `AI_Office` を開いている）＝
1つでもなければ、**先へ進まずに社長にそう伝えてください。**
展開がうまくいっていない可能性があります。

**STEP 0 の「2」から来たとき**（社長がご自分のフォルダを開いていて、比較を終えた）＝
**5つが揃っていなくて構いません。** 社長が「入れない」と決めたものは、ないのが正しい状態です。
**ただし `Shared_Assets/` だけは、必ずあるはずです**（比較の「B そのまま置くもの」で移動しました）。
**なければ、そこへ戻って移動してから進んでください。** 会社の情報やロゴの書き込み先になります。

### 2. `CLAUDE.md` の「AIオフィスの場所」を埋める（③）

いま開いているフォルダの場所を、そのまま書いてください。
**ここは社長に聞かなくても分かる唯一の項目です**（`SETUP_STATUS.md` の③）。

### 3. 禁止事項が、このパソコンでどこまで効くかを確かめる（社長に見せる作業はありません）

**2026-09-15に、社長へ「試して見せる」工程を外しました。** 理由は2つです。

- **拒否されたとしても、証明できるのは「その命令が拒否される」ことだけ**で、
  「社長が守られている」の証明にはならない。**同じことができる別の命令が残っている**
- **Claude Code が `WebFetch` など別の手段で代替すると、設定が正しくても不合格に見える**

**フォルダの取り違えは、STEP 0 で捕まえています**（直下に `SETUP_STATUS.md` があるかを見る）。
設定ファイルが読み込まれていないという事態は、そちらで防いでください。

**あなたがここでやることは1つだけです。社長のパソコンの種類を確かめてください。**

**Windows では `PowerShell`、Mac では `Bash` が使われます**（公式ドキュメントに明記されています）。
**Windows に `Git for Windows` が入っていると `Bash` も使えるようになりますが、
`PowerShell` が使えなくなるわけではありません。両方が使える状態になります。**

**Windows のときは、削除の禁止が1つも効きません。** この事実は、STEP 2 の手順3の最後（復元テストの報告）で社長に伝えます。
**ここでは何も言わないでください**（6番までまとめて伝えます）。

`SETUP_STATUS.md` の**⑨**に、確かめた結果（どちらのパソコンか）を書いてください。
**⑧ではありません。**⑧は、手順3の最後に伝える削除の扱いと、外部サービスとアプリの機能の禁止の2つです。
**⑨が「済」になるのは、その結果を社長に伝えたあと**（STEP 2 の手順3の最後）です。
**ここではまだ「未」のままにしてください。**

---

## STEP 2　READMEの手順どおりに進める

**ここからが、社長がREADMEの第3部を見ながら進める部分です。**
**READMEと同じ順番で進めてください。** 社長はREADMEを読みながら見ています。
**順番が違うと、故障だと思われます。**

> **書き込む先は、いつも「いま開いているフォルダ」の側です（2026-09-15に追加）。**
>
> STEP 0 の「2」から来たとき（社長がご自分のフォルダを開いている）は、
> **`CLAUDE.md` も `Shared_Assets/` も、`AI_Office/` の中と、いま開いているフォルダとに
> 2つ存在します。** 下のどの手順でも、**書くのは、いま開いているフォルダの側だけ**です。
>
> **`AI_Office/` の中のファイルには、1文字も書かないでください。**
> **あれは見本です。最後に消えます。そこへ書いたものは、一緒に消えます。**
>
> `AI_Office/CLAUDE.md` には「〈 〉の欄を埋めてください」と書いてありますが、
> **それは①の読者に向けた指示です。②では、その見本を埋めないでください。**

| README 第3部の見出し（番号＝進捗の表の番号） | ここでの節 |
|---|---|
| 1〜2　会社の情報を知らせる | 手順1 |
| 3〜6　保存先とファイル名を決める | ③は STEP 1 の2、④は手順2、⑤⑥は手順4で確かめる |
| 7〜9　変更履歴（Git） | 手順3（⑦のあと、最後に⑧⑨を伝える） |
| 10　共通資料 | 手順4 |
| 11〜12　ロゴとブランドカラー | 手順5 |
| 13〜18　残りの6項目 | STEP 3 のあと（`office-setup`） |

**ここでの節の名前（手順1〜5）は、このファイルの中だけの呼び名です。社長には使わないでください。** 社長に伝えるときは、左の列の番号と言葉を使います。

その直後に、次の一言を**1回だけ**伝えてください。ここから社長への質問が始まるからです。
この一言は、あとの STEP では繰り返しません。

```
これから、いくつか質問をします。
私の質問の意味が分からないときは、遠慮なく「どういう意味？」と聞いてください。
別の言い方で、もう一度お聞きします。
```

質問は、`.claude/rules/work-style.md` の「社長に質問するときの書き方」を読んで、
そのとおりに書いてください（何について聞くのか・答えを何に使うのか・どう答えればよいのか）。

### 手順1　会社の情報を、ホームページか名刺から読み取る（①②）

```
資料やメールに会社の情報を正しく書けるように、会社のことを教えてください。
会社のホームページか、社長のプロフィールが載っているページのURLを、
この会話に貼り付けてください。
（URLのコピーのしかた：ブラウザでそのページを開き、画面のいちばん上にある
　アドレスの欄を右クリックして「コピー」を選びます）

ホームページがなければ、名刺1枚の写真でも構いません。
スマートフォンで名刺を撮影し、その写真をご自分あてにメールで送るのが、いちばん簡単です。
パソコンでそのメールを受け取って写真を保存したら、その写真をこの会話に入れてください。
入れ方は2つあります。やりやすいほうで構いません。

（1）写真のファイルを、この会話の文字を入力する欄の上まで引っぱって、
　　 マウスのボタンを離す（ドラッグアンドドロップ）
（2）写真のファイルを右クリックして「コピー」を選び、
　　 文字を入力する欄で右クリックして「貼り付け」を選ぶ
```

**どちらもない、と言われたときは、次の5つを1回でうかがってください。**
**1つずつ聞かないでください。** ここは社長が打ち込む作業なので、
往復を増やすと手が止まります。

```
ホームページも名刺もないのですね。承知しました。
では、次の5つを直接うかがいます。

　会社名
　代表者名
　住所
　電話番号
　メールアドレス

分からないものや、まだ決まっていないものは、「未定」とお答えください。
決まったときに教えていただければ、あとから私が書き足します。
```

**「未定」と言われた項目は、そのまま「未定」と書きます。推測で埋めないでください。**
この場合、読み取りの確認（次のブロック）は行いません。**社長ご自身が答えた内容なので、
読み違いが起きないためです。** そのまま `Shared_Assets/Company_Info.md` と
`CLAUDE.md` に書き込み、書き込んだ内容を画面に示して次へ進んでください。

読み取った内容で `Shared_Assets/Company_Info.md` と `CLAUDE.md` の会社情報を埋め、
社長に確かめてもらいます。**あなたの読み違いを防ぐためです。**

```
〈ホームページ／名刺〉から、会社の情報を次のように読み取りました。
違っているところがあれば、その部分だけ、正しい内容を教えてください。
すべて合っていれば、「はい」とだけお答えください。

〈会社名／代表者／住所／電話／メール〉
```

それでも埋まらない項目は、**「未定」と書いて残します。**
**推測で埋めないでください。** 「未定」の項目は、使う場面が来たときに聞きます。

### 手順2　保存先とファイル名を決める（④⑤⑥）

**決めるのは「これから作るファイル」の置き場所だけです。**
社長がいま別の場所に保存しているファイル（「ドキュメント」フォルダやデスクトップにあるもの）は、
いまは対象外です。

**社長が不安になる場面なので、聞かれる前に先に伝えてください。**

```
これから私と一緒に作るファイルは、すべていま Claude Code で開いているフォルダ（〈フォルダの場所〉）の
中に保存されます。

いま別の場所に保存しているファイルも私に扱ってほしいときは、
そのときにおっしゃってください。方法は2つあります。

（1）そのファイルを、いま Claude Code で開いているフォルダの中へ移動する
　　 エクスプローラー（画面の下にある、フォルダの絵のボタン）で、
　　 ファイルを右クリックして「切り取り」を選びます。
　　 次に、いま Claude Code で開いているフォルダを開いて、何もないところで右クリックし、「貼り付け」を選びます。
　　 ファイルの数が少ないときは、こちらが簡単です。

（2）そのファイルが保存されているフォルダを、フォルダごと私が読めるように設定する
　　 私に「〈フォルダの名前〉も読めるようにしてください」とおっしゃってください。
　　 設定は私が行うので、社長が操作する必要はありません。
　　 ファイルの数が多いときや、ファイルの置き場所を変えたくないときは、こちらが便利です。
```

**（2）を頼まれたときの実体（2026-09-16に公式ドキュメントで確認）**：
`/add-dir <パス>` を実行すると、そのセッションのあいだ読めるようになります。
毎回効かせたいときは、`.claude/settings.json` の
`permissions.additionalDirectories` にそのパスを足します。
**どちらもあなたが行います。社長にコマンドを打たせないでください**（大原則①）。
**足す前に、必ず社長の承認を取ってください**（読める範囲が広がるためです）。

**この場では決めません。** 開設の作業を止めないでください。

### 案の出し方は、フォルダの中を見て決めます

**社長に「どちらですか」と聞かないでください。見れば分かります。**

| いま開いているフォルダの中身 | どちらの案を出すか |
|---|---|
| **仕事のファイルが1つもない**（キットのファイルだけ）＝STEP 0 の「1」から来た | **下のA**（新しく3つ作る案） |
| **ファイルを分けたフォルダが、すでにある**＝STEP 0 の「2」から来た | **下のB**（社長の分け方に合わせる案） |

**どちらも「案を出す → 違うところだけ言っていただく」という同じ形**にしてあります。
READMEは「案が示されるので、変えたいところだけ指摘してください」としか
書いていないので、**どちらでも読者の動作は変わりません。**

**A　新しく作る案**

```
これから Claude Code を使って社長が作るファイルの保存場所を、次のように決めたいと思います。
どのフォルダも、いま Claude Code で開いているフォルダの中に作ります。

  作業の途中のファイル　→ 〈作業中〉
  完成したファイル　　　→ 〈成果物〉
  お客様ごとの資料　　　→ 〈顧客〉フォルダの中に、お客様ごとのフォルダを作る

いまあるファイルは、移動も削除もしません。
この決め方でよろしいですか。変えたいところがあれば、その部分だけおっしゃってください。
```

**B　社長の分け方に合わせる案**

```
いま Claude Code で開いているフォルダの中を確認しました。
社長はファイルを、すでに次のフォルダに分けて保存しておられるようです。

　〈読み取ったフォルダ名を、そのまま並べる〉

これから Claude Code を使って社長が作るファイルも、この分け方に合わせて保存します。
具体的には、次のようにしたいと思います。

  作業の途中のファイル　→ 〈社長のフォルダ名〉
  完成したファイル　　　→ 〈社長のフォルダ名〉
  お客様ごとの資料　　　→ 〈社長のフォルダ名〉

新しいフォルダは作りません。
この決め方でよろしいですか。別の判断があれば、その部分だけおっしゃってください。
```

**Bでは、社長のフォルダ名をこちらの言葉に置き換えないでください。**
社長が `01_作業中` と名付けているなら、`CLAUDE.md` にも**その名前のまま**書きます。
**当てはまるフォルダが見つからない項目は、空欄のまま社長に尋ねてください**
（推測で割り入れないでください）。

**社長が「はい」と言うまで、フォルダを作ったり、ファイルを動かしたりしないでください。**
**Bでは、新しいフォルダを作りません。**

### 手順3　変更履歴（Git）の記録を開始する（⑦）　←　ここを飛ばさない

**READMEは社長に「消したファイルを元に戻せます」と約束しています。**
**その約束を成り立たせるのが、この手順です。** ここを飛ばすと、社長は
戻せると思い込んだまま、戻せない状態で使い続けることになります。

**Git は必須です（2026-09-15に変更）。** 入っていない環境では、
**入れていただくまで、次の手順へ進みません。**

#### まず、3つを機械で確かめる（社長には聞きません・2026-09-16に追加）

**手順3に入ったら、最初に次の3つを実行して、すでに済んでいるものを調べてください。**
**済んでいるものは飛ばし、足りないものだけを行います。**

| 確かめるもの | 実行するコマンド | すでに済んでいたら |
|---|---|---|
| Git がこのパソコンに入っているか | `git --version` | 下の「インストールされていなかったときの案内」を飛ばす |
| このフォルダで、変更履歴の記録がもう始まっているか | `git rev-parse --is-inside-work-tree` | `git init` を飛ばす |
| 記録する人の名前とメールが設定されているか | `git config user.name` と `git config user.email` | 設定を飛ばす（社長がお使いの設定が正です） |
| **ファイルを削除して、元に戻す復元テスト** | ー | **飛ばしません。必ず行います** |

**復元テストだけは、3つとも済んでいても飛ばさないでください。**
**記録が始まっていることは、ファイルを元に戻せることの証明になりません。**
2026-09-16の実機テストで、Git は入っていて、設定も正しく、記録も始まっていたのに、
**元に戻す操作だけが拒否されました**（下の「復元テスト」を読んでください）。
「設定が正しいから飛ばす」と判断していたら、この不具合は見つからないまま読者に届いていました。

**3つとも済んでいたときは、社長にこう伝えてから、復元テストへ進みます。**

```
このパソコンには、変更履歴を記録する仕組み（Git）が、すでにインストールされていました。
Git を使うための設定も済んでいたので、インストールし直す必要はありません。

ここからは、変更履歴を使って本当にファイルを元に戻せるかを確かめる、復元テストを1回行います。
私がテスト用ファイルを1つ作って削除し、変更履歴を使ってそのファイルを元に戻し、
中身が削除する前と同じかを確かめて、結果をご報告します。社長の操作は要りません。
```

**推測で「入っている」と書かないでください。** 必ず上のコマンドを実行して確かめてください。
**入っていなかったとき**は、下の「インストールされていなかったときの案内」を出して、**ここで止まります。**

#### 記録の開始のしかた（この順で行います）

**いま開いているフォルダが、すでに変更履歴の下にあることもあります**
（②の読者＝ご自分で環境を作ってこられた方に多い）。**確かめずに開始しないでください。**

1. **`git rev-parse --is-inside-work-tree` を実行します。**
   - `true` が返った → **すでに始まっています。`git init` を実行しないでください。**
     そのまま3へ進みます
   - 失敗した（エラーになった）→ **まだ始まっていません。**2へ進みます
2. **`git init` を実行します。** いま開いているフォルダで行ってください
   （`.gitignore` は最初から入っているので、鍵になるファイルは記録されません）
3. **下の「名前とメールの設定」を済ませます**（ここを飛ばすと4で失敗します）
4. **最初の記録を残します。** `git add` で**このフォルダの中のファイルを名指しで**加え、
   `git commit` で記録します。**`git add -A` は使わないでください。**
   ほかの画面で書きかけの作業まで巻き込みます

**2で `git init` を実行したときは、社長に1行だけ伝えてください。**

```
このフォルダで、変更履歴の記録を開始しました。
これから先は、私がファイルの状態を記録するたびに、
その時点の状態へ、あとから戻せるようになります。
```

#### 名前とメールの設定（**最初の記録の前に、必ず行う**）

**Git は、記録する人の名前とメールが設定されていないと、最初の記録で失敗します**
（`Author identity unknown` と出ます）。**入れたばかりのパソコンでは、まず設定されていません。**

**何を登録するかは、あなたが提案して、社長の承認を取ってください（2026-09-16に変更）。**
**「誰の名前を登録すればよいですか」と白紙で聞かないでください。** 非IT系の社長は、ここで手が止まります。
手順1で、代表者名とメールアドレスはもう教えていただいています。それを案として出します。

1. `git config user.name` と `git config user.email` を実行して、値が返るかを見ます
2. **どちらかが空のときだけ**、下の文で、手順1で得た**代表者名**と**メールアドレス**を
   登録してよいかを聞きます。「はい」と言われたら、**このフォルダにだけ**設定します
   （`--global` を付けないでください。**社長のパソコン全体の設定を書き換えないため**です）

```
変更履歴を記録するには、記録した人の名前とメールアドレスを、Git に登録する必要があります。
先ほどうかがった社長のお名前（〈代表者名〉）とメールアドレス（〈メールアドレス〉）を、
Git に登録してよろしいですか。
この情報は、このパソコンの中の変更履歴に記録されるだけです。
このキットの手順の中で、インターネットに送信されることはありません。
```

   別の名前やメールアドレスを指定されたら、そちらを登録します。

```
git config user.name "〈代表者名〉"
git config user.email "〈メールアドレス〉"
```

3. **メールアドレスが「未定」のとき**は、上の文の代わりに次の文で聞き、
   「はい」と言われたら `未定@example.invalid` を登録して先へ進みます。**ここで止まらないでください。**

```
変更履歴を記録するには、記録した人の名前とメールアドレスを、Git に登録する必要があります。
先ほどうかがった社長のお名前（〈代表者名〉）を登録し、
メールアドレスはまだ決まっていないので、仮のメールアドレスを登録してよろしいですか。
この情報は、このパソコンの中の変更履歴に記録されるだけです。
このキットの手順の中で、インターネットに送信されることはありません。
メールアドレスが決まったら、私に教えてください。いつでも登録し直します。
```

**すでに値が入っているときは、触らないでください。** 社長がお使いの設定が正です。

#### インストールされていなかったときの案内

```
変更履歴を記録する仕組み（Git）が、このパソコンにはまだインストールされていません。
Git がないと、私が誤って削除したファイルを、元に戻すことができません。
そのため、パソコンに Git をインストールしておくことは必須です。

Git のインストールは、社長ご自身に、このパソコンで行っていただく必要があります
（私の側からは、インストールを実行できません）。
少し手間がかかりますが、一度だけの作業です。

「Git のインストールのしかたを教えてください」とおっしゃってください。
画面を一緒に見ながら、1つずつご案内します。

インストールが終わったら、次のようにおっしゃってください。
「Git をインストールしました。変更履歴の記録を開始してください」
```

**「インストールのしかたを教えてください」と言われたときの進め方**

- **手順をまとめて読み上げないでください。** インストーラーの画面はバージョンで変わります。
  **社長がいま見ている画面を聞いてから、次の1手だけを伝えてください。**
- 入口だけは先に伝えて構いません（Windows＝`git-scm.com` の配布ページ、
  Mac＝ターミナルで `xcode-select --install`）。**その先は画面を確かめながら進めます。**
- ニュースレター第9号にも書いてあります。社長がそちらを見たいと言われたら、それで構いません。
- **インストールの実行を、あなたが引き受けようとしないでください。** できません。

**社長が「Gitを入れました」とおっしゃるまで、次の手順へ進まないでください。**
おっしゃったら、`git --version` をもう一度実行して確かめます。

#### 入れたのに `git --version` がまた失敗するとき（**よく起きます**）

**Claude Code のアプリは、起動したときのパソコンの設定を持ったまま動いています。**
**あとから入れたものは、アプリを開き直すまで見えません。**
**入れ方が悪かったのではありません。**

**「もう一度インストールしてください」と言わないでください。** 何度やっても同じです。

```
Git のインストールは、できています。
ただ、いま動いている Claude のアプリは、Git をインストールする前に起動したので、
まだ Git を見つけられない状態です。

お手数ですが、Claude のアプリをいったん終了して、もう一度起動してください。
ここまでに決めていただいた内容は、すべてこのフォルダに保存されているので、消えません。

アプリを起動し直したら、このフォルダを開いて、次のようにおっしゃってください。
「オフィス開設の続きをやりましょう」
```

**開き直したあとは、`SETUP_STATUS.md` を読んで、続きから進めます**
（`.claude/skills/office-setup/SKILL.md` を開いて、そのとおりに進めてください）。
**最初からやり直させないでください。**

#### 復元テスト（社長の操作は要りません）

記録を開始したあと、**実際に次の4つを行ってください。**

1. テスト用ファイルを1つ作って、記録する
2. そのファイルを削除する
3. 記録から元に戻す
4. 戻したファイルの中身が、削除する前と同じことを確かめる

**「お見せします」「ご覧いただけます」と言わないでください（2026-09-16・耕治さんの指示）。**
社長の画面に出るのは、実行したコマンドと、あなたの報告の文だけです。
ファイルが消えて戻る様子を目で見られるように期待させてしまいます。**行ったことと、確かめた結果を報告してください。**

**3の戻し方は、次のコマンドを使ってください（2026-09-16に指定）。**

```
git show HEAD:<ファイル名> > <ファイル名>
```

- **Bash で実行してください。PowerShell では実行しないでください。**
  Windows の PowerShell 5.1 は、`>` で書き出すときに文字コードを変えてしまうため、
  中身の同じファイルに戻りません
- 実行したら、**`git status` を実行して、何も表示されないことを確かめてください。**
  何か表示されたら、**元に戻ったことにしないでください**
- **ほかの戻し方を第一候補にしないでください。** 作業ツリー全体を戻す種類のコマンドは、
  書き方を誤ると、社長がまだ記録していないほかの作業まで消します。
  上のコマンドは**名指しした1ファイルだけ**を書き戻すので、ほかの作業に触りません

**社長がご自分で見張りの仕組み（フック）を入れている環境では、元に戻す操作が拒否されることがあります。**
拒否されたら、**画面に出た文言をそのまま社長にお見せして、どうするかを聞いてください。**

- **Git の故障ではありません。** 社長が入れた仕組みが、拒否しているだけです
- **同じコマンドを「もう一度やってみます」と繰り返さないでください**
- **そのフックは、コマンドの文字列だけを見て拒否することがあります。** Git を使っていない
  書き込み（記録を残す作業など）まで拒否されたら、**シェルを使わず、ファイルを書き込む
  ツールで書いてください**

**「記録した」だけでは、戻せる証拠になりません。**
**書き換えを戻して見せても、確かめたことになりません。**
社長への約束は「**消した**ファイルを元に戻せる」なので、**実際に消して戻してください。**

4まで終えたら、社長に下の文を伝えて、**返事を待ってください。**
**この文で、ファイルの削除の扱い（⑧の1つ目と⑨）も一緒に伝えます**（2026-09-16に、質問から、結果を伝える形に変更）。

**なぜ質問にしないのか**：以前は「（1）このままにする／（2）削除をすべて禁止する」と聞き、（1）をおすすめしていました。
おすすめがいまの状態と同じなので、答えても何も変わらず、社長には「なぜ聞かれたのか」が分かりませんでした
（耕治さんの実機テストでの指摘）。**変更履歴ができて元に戻せるようになった、という流れの中で結果を伝え、
禁止したい社長だけが、ひと言伝える形にしました。**

**⑦は、ここへ来る時点で必ず「済」になっています**（Git を必須にしたため）。
**したがって「元に戻せるようになりました」と言って構いません。**

**Windows のとき**

```
【報告】
README 第3部の7〜9「変更履歴（Git）の記録を開始し、履歴から復元できることを確認する」が終わりました。
変更履歴の記録を開始し、私がテスト用ファイルを1つ作って削除し、
変更履歴を使って元に戻す、復元テストを行いました。
戻したファイルの中身が、削除する前と同じことを確かめました。
これで、私がファイルを誤って削除しても、元に戻せるようになりました
（戻せるのは、変更履歴に記録した時点の内容です）。

そのため、ファイルの削除は、いまの設定のまま私にも任せられるようにしておきます。
不要になったファイルの削除や、フォルダの整理を、私に頼むことができます。

とはいえ、ファイルの削除は重要な影響を与える操作です。
AIに削除の権限は与えたくない、と思われるかもしれません。
そのため、私がファイルを削除する前には、必ず社長の承認を得るようにします。

それでも私にファイルを削除させたくない場合は、「ファイルの削除を禁止してください」とおっしゃってください。
なお、Windows では「まとめて強制的に削除する操作だけ」を禁止することはできないため、
禁止する場合は、私が行うファイルの削除をすべて禁止します
（そのときは、ファイルの削除や整理を、社長ご自身で行っていただくことになります）。

次は、第3部の10「共通資料を、Claude Code が必ず参照するようにする」です。
次の手順に進んでよろしいですか？
```

**Mac のとき**

上の文の「なお、Windows では…」から「…社長ご自身で行っていただくことになります）。」までを、次の文に差し替えます。

```
なお、フォルダやファイルをまとめて強制的に削除する操作と、
接続用のIDやパスワードが入ったファイルを読む操作は、すでに私に禁止してあります。
禁止する場合は、私が行うファイルの削除をすべて禁止します
（そのときは、ファイルの削除や整理を、社長ご自身で行っていただくことになります）。
```

**Windows では、削除の禁止が1つも効いていません。** 上の「なお、Windows では…」の文で、そのことを必ず伝えてください（⑨）。
**`Git for Windows` が入っていても同じです。** `Bash` 向けに書いた禁止は、`PowerShell` のほうから通ってしまいます。
`PowerShell` は同じ操作を何通りにも書けるため、「強制的にまとめて削除する操作だけ」を書き方で塞ぎきれません。

**「削除する前に、必ず社長の承認を得る」は、社長への約束です。必ず守ってください。**
この約束は `.claude/rules/safety.md` の「必ず社長の確認を取る操作」に書いてあります（禁止の設定ではなく、あなたが守る決まりです）。

結果は `SETUP_STATUS.md` の⑦に記入します。**「保留」は使いません**（必須にしたため）。
⑧の1つ目には「**削除の扱いを伝えた。禁止の希望は〈なし／あり〉**」、⑨には「**〈Windows／Mac〉。削除の禁止が効く範囲を伝えた**」と書きます。

**戻せることを確かめるときは、書き換えではなく、実際に削除して戻してください。**
社長への約束は「**削除した**ファイルを元に戻せる」です。
書き換えを戻しても、その約束を確かめたことになりません。

#### 「ファイルの削除を禁止してください」と言われたら

**`.claude/settings.json` の禁止事項に削除の操作を足し、「足しました」と伝えてください。**
書き方の見本は `.claude/settings-template.jsonc` の「1. 重要なファイルの削除」にあります。
**Windows では、`Bash` 用と `PowerShell` 用を必ず両方書いてください。** 片方だけだと、もう片方から通ります。
⑧のメモ欄を「禁止の希望あり。足した」に書き換えます。

**拒否されるところを見せないでください（2026-09-15）。** 拒否されたとしても示せるのは
「その命令が拒否される」ことだけで、**同じことができる別の命令は残っています。**
見せると「もう安全だ」と受け取られるので、**かえって危険です。**
伝えるのは、足したという事実と、**それでも100%ではないという一言**です。

この言葉は、手順3のあとに言われることもあります。**いつ言われても、同じように行ってください。**

**手順3で、社長に判断を求めることはありません。** ほかの設定について、社長に意見を求めないでください。
**例外は1つだけです。** STEP 0 の「2」から来た社長（これまで Claude Code を使ってきたフォルダを開いている社長）には、
下の「外部サービスとアプリの機能を、最初は禁止する」で、**1問**を聞きます（2026-09-16に変更）。

### ⑧には、2つを書きます

**⑧に記録するのは、次の2つです。両方書いて「済」にしてください。**

| | 何を書くか | 誰が決めるか |
|---|---|---|
| **1つ目** | **ファイルの削除の扱いを伝えた結果**（禁止の希望があったか） | **あなた**が伝え、禁止するかは**社長**が決める（希望があったときだけ） |
| **2つ目** | **外部サービスとアプリの機能を禁止した結果**（禁止した名前の一覧と、確かめた結果） | STEP 0 の「1」から来たとき＝**あなた**（社長に聞きません）／「2」から来たとき＝**社長**（質問への答え） |

### 外部サービスとアプリの機能を、最初は禁止する（2026-09-16に追加）

**なぜ**：キットを開いただけのフォルダでも、Gmail・Google Drive などの外部サービスや、
Chrome の操作・画面の操作・定時実行を、あなたが使える状態になっていることがあります。
キットが足したものではありません。claude.ai のアカウントにつないだコネクタ、
パソコンの利用者全体の設定、デスクトップアプリの内蔵機能から来ます（2026-09-16の実機テストで確認）。
**AIオフィスの開設作業には、どれも使いません。** 使う場面が来るのは実践編なので、
**最初は禁止しておき、使う場面が来たら1つずつ使えるようにします**（担当は `.claude/skills/connect-check/`、定時実行は `.claude/skills/automation-setup/`）。

**禁止の行は、キットの `settings.json` に最初から書いてありません。** コネクタの名前は読者ごとに違う可能性があり、
アプリの版や Mac では内蔵機能の名前が違うこともあるため、**この会話で実際に使える名前を、あなたが読み取って書きます。**

#### 1. いまの会話で使えるものを、名前で書き出す

あなたが呼び出せるツールのうち、**名前が `mcp__` で始まるものを全部**書き出してください。
**読み込み済みのものだけでなく、名前だけが示されていて、使う前に読み込みが必要なもの（deferred）も含めます。**
`mcp__〈サーバー名〉__〈機能名〉` の〈サーバー名〉ごとにまとめます。

#### 2. 禁止するもの・残すものに分ける

| 分け方 | 対象 | 書き方 |
|---|---|---|
| **サーバーごと禁止** | claude.ai のコネクタ（Gmail・Googleカレンダー・Google Drive など、名前に長い英数字が付くことが多い）／利用者全体の設定で入っている外部サービス／Chrome の操作（`claude-in-chrome`）／画面の操作（`computer-use`）／別セッションの操作（`ccd_session_mgmt`）／つなぎ先のオン・オフ（`ccd_connectors`）／作業フォルダの切り替え（`ccd_directory`）／GitHub の PR の操作（`ccd_pr`）／定時実行（`scheduled-tasks`） | `"mcp__〈サーバー名〉__*"` |
| **機能ごと禁止** | アプリ内蔵のブラウザのうち、クリック・フォームへの入力・ページ内でのプログラム実行 | `"mcp__〈サーバー名〉__computer"`・`"mcp__〈サーバー名〉__form_input"`・`"mcp__〈サーバー名〉__javascript_tool"` など、名前を1つずつ |
| **残す** | 画面の中で完結するもの（作業の区切りの表示・提案のカード・図や表の表示・画面の切り替え）／内蔵ブラウザでページを開いて読むこと／ターミナルの表示を読むこと／コネクタを探す（検索して候補を表示するだけ） | 書かない |

**迷ったら、禁止する側に入れてください。** 使えるようにするのは、あとからいつでもできます。

**`"mcp__*"` のように、全部まとめて禁止する書き方は使わないでください。**
`allow` で `deny` の例外は作れない（公式ドキュメント https://code.claude.com/docs/en/permissions ）ため、
まとめて禁止すると、あとで「Gmail だけ使えるようにする」ことができなくなります。

#### 3. STEP 0 のどちらから来たかで、進め方を変える

| 来たところ | 進め方 |
|---|---|
| **「1」**（社長が `AI_Office` を開いている） | **社長に聞かずに、`.claude/settings.json` の `deny` に書き込みます。** 書き込んだあと、下の「4」で確かめ、**結果を下の「報告の文」で伝えます** |
| **「2」**（社長が、これまで使ってきたフォルダを開いている） | **書き込む前に、下の「質問」で聞きます。** このフォルダで、社長がいま実際に使っているサービスが含まれているからです。**黙って禁止すると、昨日までできた操作が、理由の分からないまま急にできなくなります** |

**書き出した結果、禁止するものが1つもないとき**は、⑧のメモ欄に「**禁止するものはなかった**」と書いて、報告も質問もしません。

**報告の文（STEP 0 の「1」から来たとき）**

**名前は、読み取ったものを言い換えずに並べてください。** ただし、長い英数字のコネクタ名は、
そのコネクタの説明文から分かるサービス名（Gmail など）を括弧で添えます。

```
このAIオフィスでは、私（Claude Code）が次のサービスや機能を使うことを、最初は禁止しました。

　〈禁止したサービスや機能の名前を、そのまま並べる〉

いずれも、メールの送信、予定の招待、ファイルの共有、Chrome や画面の操作など、
社外の人に内容が届いたり、社長の代わりにパソコンを操作したりできる機能です。
AIオフィスの開設作業には使わないので、禁止しても開設作業に影響はありません。

使いたいサービスが出てきたら、「〇〇を使えるようにしてください」とおっしゃってください。
使えるようにする前に、そのサービスで私に何をさせてよいかを、一緒に確認します。
なお、この禁止で、すべての危険な操作を防げるわけではありません。
```

**質問（STEP 0 の「2」から来たとき）**

```
いま、このフォルダでは、私（Claude Code）が次のサービスや機能を使える状態になっています。

　〈禁止する候補の名前を、そのまま並べる〉

いずれも、メールの送信、予定の招待、ファイルの共有、Chrome や画面の操作など、
社外の人に内容が届いたり、社長の代わりにパソコンを操作したりできる機能です。
このフォルダで、私がこれらを使うことを禁止するかどうか、決めていただけますか。

（1）すべて禁止する
　　よい点：私が誤って、社長の確認なしにメールを送信したり、ファイルを共有したりすることを防げます。
　　注意点：いまこのフォルダで使っているサービスも、私が使えなくなります。
　　　　　　使いたくなったら、1つずつ使えるようにできます。

（2）いま使っているサービスだけ残し、ほかは禁止する
　　→ 残したいサービスの名前を教えてください。
　　注意点：残したサービスについては、このキットのルールで、メールの送信や支払いの前に、
　　　　　　私が必ず社長に確認することになっています。ただし、ルールは私への指示なので、
　　　　　　私が守り損ねると、社長の確認なしにメールを送信したり、支払いをしたりしてしまうおそれがあります。

おすすめは（1）です。いま使っているサービスは、この開設作業が終わったあとで、
何をさせてよいかを一緒に確認しながら、1つずつ使えるようにできます。
なお、禁止しても、すべての危険な操作を防げるわけではありません。
```

**（2）の注意点を省かないでください。** ルール（`.claude/rules/safety.md`「必ず社長の確認を取る操作」）は
あなたへの指示であって、禁止の設定ではありません。

#### 4. 書き込んだら、あなたが確かめる（社長には見せません）

1. 禁止したサーバーごとに、**読み取るだけの機能を1つ**呼び出してください（一覧を表示するだけの機能など）。
   **送信・書き込み・削除をする機能は、確かめるためでも呼び出さないでください**
2. `Permission to use 〈名前〉 has been denied.` のように、**拒否されたことを示す文言が返れば、効いています**
3. **拒否されなかったものがあれば、「禁止しました」と言わないでください。** 名前を見直して書き直し、
   それでも拒否されなければ、その事実を社長に伝え、⑧のメモ欄にも書きます
4. **書き換えは、アプリを開き直さなくても効きます**（2026-09-16の実機テストで確認）。社長に開き直しを頼まないでください

**書き込みが、社長ご自身が入れたフック（見張りの仕組み）に拒否されることがあります。**
拒否されたら、**画面に出た文言をそのまま社長にお見せして、どうするかを聞いてください。**
コマンドで書き戻すのではなく、ファイルを書き込むツールで書くと通ることがあります。

#### 5. ⑧のメモ欄に書くこと

- 禁止した名前の一覧（サーバーごと禁止か、機能ごと禁止か）
- 4で確かめた結果（拒否されたもの／拒否されなかったもの）
- STEP 0 の「2」から来たときは、社長の答え（（1）か（2）か、残したサービスの名前）

### 手順4　共通資料を参照する指示を書き込む（⑩⑤⑥）

**社長に聞くことはありません。あなたの作業です。**

1. **`Shared_Assets/README.md` を開いてください。** その中に、社長からあなたへの
   指示文（プロンプト）が書かれています。**その指示に従って、4つの共通資料を
   いつ参照するかを `CLAUDE.md` に書き込んでください**（`SETUP_STATUS.md` の⑩）
2. 次の2つは、**キットの既定のままで構いません。** ただし
   **中身を実際に開いて確かめてから**、`SETUP_STATUS.md` に「済」と記入してください
   - ⑤ ファイル名のルール（`YYMMDD_内容`）
   - ⑥ メモリと記録に残すことの方針

**開かずに「済」と書かないでください。**

### 手順5　ロゴとブランドカラーを聞く（⑪⑫）

```
会社のロゴと、会社の色（ブランドカラー）を教えてください。
これを登録しておくと、私が資料を作るときに、
会社のロゴと色を使って、統一感のあるデザインで作成できます。

・ロゴ：ロゴの画像ファイルがあれば、エクスプローラー（Mac では Finder）を使って、
　　　　いま Claude Code で開いているフォルダの中にある「Shared_Assets」フォルダへ、
　　　　その画像ファイルを入れてください。
　　　　入れ終わったら、「入れました」とおっしゃってください。
・色　：会社の色が決まっていれば、色の名前か、色の番号を教えてください
　　　　（例：濃い赤、#C62828）
```

> **ロゴは、この会話に貼り付けてもらわないでください（2026-09-15に変更）。**
> **貼り付けられた画像を、あなたはファイルとして保存できません。**
> 会話の中では見えていても、`Shared_Assets/` へ置くことはできないので、
> **「置きました」と言えば嘘になります。**
> **社長の手でフォルダへ入れていただくのが、唯一確実な方法です。**
>
> **「入れました」と言われたら、`Shared_Assets/` の中を実際に見て、
> 画像ファイルがあることを確かめてから「済」にしてください。**
> 見つからなければ、そう伝えて、もう一度置いていただきます。

**まだ決まっていない、ファイルが手元にない、というのは、よくあることです。**
そのときは、こう伝えて**次へ進んでください。**

```
承知しました。ロゴや会社の色が用意できたら、そのときに私に教えてください。
それまでは、私が資料を作るときに、ロゴと会社の色を使わずに作ります。
```

**保留にしたことと理由を、`SETUP_STATUS.md` の「保留にしたものと、その理由」に
1行書いてください。** 書かない保留は、忘れられた保留と同じです。

置き場所は、次のとおりです。

- ロゴの画像 → `Shared_Assets/` の直下（**社長が置きます。**ファイル名はそのままで構いません）。
  **あなたは、置かれたことを確かめて、ファイル名を `Design_Rules.md` に書きます**
- 色 → `Shared_Assets/Design_Rules.md` の「色」の欄（**あなたが書きます**）

**ここで聞くのは、ロゴと色だけです。** 文字の大きさ・見出しの付け方・余白といった
**資料の作り方は、まだ決めません。** 何を作るかが決まってからでないと、正解が決まらないためです
（`SETUP_STATUS.md` の「使う場面が来たら」に入っています）。

---

## STEP 3　進捗の表を数え直して、このまま続けるかを社長に聞く

### 締めくくる前に、業務の言葉で3点だけ報告する

**業務の言葉で3点だけ**報告します。技術用語を使わないでください。

```
AIオフィスの準備ができました。ここまでの結果をご報告します。

変わったこと：〈例：私が作る資料とメールは、会社の書式と文章の決まりに沿って作られるようになりました〉
私に禁止したこと：〈例：私（Claude Code）が、フォルダやファイルをまとめて強制的に削除する操作を禁止しました〉
困ったとき：〈例：「さっきの変更を取り消して」とおっしゃれば、私が、記録してある直前の状態にファイルを戻します〉
```

> **何回に分けるかを、あなたが決めないでください（2026-09-15・耕治さんの判断）。**
> 決めるのは社長です。**⑫まで終わった時点で、ここまでの結果を伝えて、
> 「このまま続けますか」と聞いてください。**
> READMEは、18項目を続けて進める書き方になっています。
> **「今日はここまでです」と、あなたの側から打ち切らないでください。**

`SETUP_STATUS.md` は、**1マス終わるごとにその場で書き換えてきたはず**です
（そう書いてあります）。ここでやるのは、書き忘れがないかを確かめて、数え直すことです。

**実際に確かめてから記入してください。** 推測で「済」にしないでください。
ファイルの内容を開いて見てください。

そのうえで、社長にこう伝えます。

**締めくくりの言葉は、実際にできたことだけを言ってください。**
**できていないことを、できたことのように言わないでください。**
社長はこの一言を信じて使い始めます。**ここで嘘を言うと、社長は戻せないまま
「戻せる」と思って使い続けることになります。**

| 結果 | 締めくくりで言ってよいこと |
|---|---|
| ⑦が「済」（復元テストで、実際に削除したファイルを元に戻せることを確かめた） | 「削除したファイルは元に戻せます」と言ってよい。**Git は必須にしたので、ここまで来ていれば必ず「済」です** |
| 禁止事項 | **「危険な操作はすべて禁止してあります」と言い切らないでください。** 禁止できているのは代表的な命令だけです。**Windows では、社長が削除の禁止を希望しない限り、削除を1つも禁止できていません。** 言うなら「**代表的な危険な操作は禁止してあります**」までです |
| 外部サービスとアプリの機能（⑧の2つ目） | **手順3の「4」で、拒否されることを確かめたものだけ**「〇〇を禁止してあります」と言ってよい。拒否されなかったものは、その事実を伝える |

**下の〈　〉は、あなたが埋める欄です。** 上の表を見て、**できたことだけ**を書いてください。
**この見本の文をそのまま読み上げないでください。**

```
【報告】
README 第3部の1〜12が終わりました。

会社の情報と、文章の書き方の決まりを、このAIオフィスに入れました。

〈上の表で「言ってよい」とされたものだけを、ここに書く〉
〈できていないものがあれば、その事実もここに書く〉
　例：私がファイルを誤って削除しても、変更履歴を使って元に戻せます。
　　　また、フォルダをまとめて強制的に削除する操作など、代表的な危険な操作は、
　　　私が行えないように禁止してあります。
　例（Windows で、社長が削除の禁止を希望しなかったとき）：
　　　ただし、私がファイルを1つずつ削除する操作は、このパソコンでは禁止していません。

次は、第3部の13「会社のミッション・ビジョン・行動指針にあたるものを CLAUDE.md に書く」です。このまま続けますか？
ここで中断しても構いません。ここまでに入れた内容は、すべてこのフォルダに保存されています。
続きを行うときは、Claude Code でこのフォルダを開いて、
「オフィス開設の続きをやりましょう」とおっしゃってください。いつでも再開できます。
何回に分けて進めても構いません。
```

**「続けます」と言われたら、いちばん先に
`.claude/skills/office-setup/SKILL.md` を開いて読んでください。**
⑬から先の手順は、そのファイルの中にしかありません。**読まずに⑬へ進まないでください。**
**「今日はここまで」と言われたら、そこで終わります。** どちらも正しい進み方です。

> **なぜ「開いて読んでください」と書いてあるか。**
> スキルの本文は、`CLAUDE.md` やルールと違って**自動では読み込まれません。**
> 読むかどうかを、あなた自身が決める仕組みになっています。
> **だからここで名指しします。** 自動で入ってくるのを待たないでください。

**⑬から先には、キットに同梱していないものを作る作業が2つ入っています**
（⑭＝作業の区切りで **Git の控え**を残すもの、⑮＝`CLAUDE.md` が200行に
近づいたら知らせるもの）。**社長が「続けます」とおっしゃったら、その場で作ります。**

**開設作業が終わったら（18項目がすべて「済」か「保留」になったら）、`.claude/skills/office-setup/SKILL.md` の STEP 8 の文で締めくくってください。**
**開設作業の途中では、AI従業員を作りましょう、と自分から言い出さないでください。** 開設作業が先です。

**開設作業が終わったあとは、社長がやりたい仕事を相談されたら、その仕事を担当するAI従業員を作ってよい**（2026-09-16・耕治さんの判断）。
`.claude/skills/skill-writing/` を読んで進めてください。**Claude Code は、AI従業員を作らなくても使えます。** 締めくくりの文でも、
「どんな仕事でも相談してください」を前に出し、より高度な専門職の採用のしかたはニュースレターの実践編で扱う、と伝えます。

〈N〉は、`SETUP_STATUS.md` の「開設の日」と「続きの回」にある「未」の数です。
「使う場面が来たら」のマスは数えません。

**急かさないでください。** この作業でいちばん時間がかかるのは、書くことではなく
決めることです。**社長が区切りたいとおっしゃったら、そこで終わります。**

**⑬から先へ進むときは、必ず `.claude/skills/office-setup/SKILL.md` を開いて読んでから
進めてください。** 日をあらためて「オフィス開設の続きをやりましょう」と言われたときも、
**同じように、先にそのファイルを開いてください。**

---

## すでに作ってある環境に入れるとき

**ここは、STEP 0 で `SETUP_STATUS.md` が見つからなかったときに読む章です。**

社長は、ご自分ですでに作った環境を開いています。
**そこへキットを上書きしてはいけません。社長が育てたものが正です。**

### やることは、置くことではなく、比較することです

社長には、展開した `AI_Office` フォルダを、**いま開いているフォルダの中に
そのまま入れてもらっている**はずです（READMEの第2部にそう書いてあります）。

見つからないときは、こう聞いてください。

```
私がキットの中身と比較するには、キットのファイルを、私が読める場所に置いていただく必要があります。
キットを展開してできた「AI_Office」フォルダを、いま Claude Code で開いているフォルダ（〈フォルダの場所〉）の中へ、
そのまま移動してください。

なお、AIオフィスの開設作業がすべて終わったら、「AI_Office」フォルダは削除して片づけます。
片づけ方は、そのときに私からお伝えします。
```

**社長に場所を打ち込ませないでください。** フォルダを移動してもらうだけにします。

### キットの中身は、2種類に分かれます

**先にこの区別をしてください。全部を比較するわけではありません。**

| | 何を | どうするか |
|---|---|---|
| **A 比較するもの** | `CLAUDE.md`・`.claude/settings.json`・`.claude/rules/`・`.claude/skills/` | **社長の環境に同じファイルがあるとき**だけ、違いを1件ずつ出して**判断を仰ぐ**。**ファイルごとないときはBと同じ**（下を読む） |
| **B そのまま置くもの** | `Shared_Assets/`・`Programs/`・`.gitignore` | **社長の環境になければ、そのまま移動する。** 判断は要りません |

**Bを比較しないでください（2026-09-15に追加）。** Bは会社の情報やロゴを入れる**空の入れ物**で、
社長の環境にある何かと重なりません。**比較する対象にすると、どこにも置かれないまま
`AI_Office` が消え、あとの手順が書き込む先を失います。**

> **Aでも、ファイルそのものがないときは、比較しません（2026-09-15に追加）。**
> **比較するのは「社長の環境に同じファイルがある」ときだけ**です。
> `CLAUDE.md` や `.claude/settings.json` が**そもそもない**なら、それは
> 「入れるかどうかを迷う違い」ではなく、**土台がまだない**という意味です。
> **Bと同じで、そのまま置いてください。** 置いたことは1行伝えますが、判断は求めません。
>
> **なぜ**：この2つは、あとの手順が必ず書き込む先です。
> 「入れない」と決められる形にしておくと、**書き込む先がないまま手順3・4へ進み、
> そこで止まります。**
>
> **ただし `.claude/settings.json`（禁止事項）は、この例外に入れません（2026-09-16に追加）。**
> **ないときも、必ず1行ずつ社長に聞いてください。**
> **禁止は「できることを減らす」設定**だからです。黙って置くと、
> **社長がいままでできていた操作（`curl` で何かを取ってくる、など）が、
> 理由の分からないまま急にできなくなります。**
> ほかのA（`CLAUDE.md`・`rules`・`skills`）は**足すだけ**なので、置いて構いません。
>
> **フックを作るとき（⑭⑮）に `settings.json` がなければ、そのときは作って構いません**
> （`hooks` だけの中身で始めます）。**禁止事項は、社長が「入れる」と言った行だけです。**

### 「入れない」と決められた節に、あとの手順が書き込む場面が来たら

**2026-09-15に追加。中身によって、行き先が2通りに分かれます。**

| 何を書こうとしているか | どうするか |
|---|---|
| **会社の情報**（会社名・代表者名・住所・電話・メール・大切にしていること）＝**データ** | **`Shared_Assets/Company_Info.md` の側にだけ書いて、先へ進む。**下の①の言葉を伝える |
| **あなたへの指示**（保存先のルール／ファイル名のルール／共通資料を毎回参照すること）＝**ふるまいの決まり** | **`Company_Info.md` に書いても効きません。**下の②か③で処理する |

**なぜ分けるのか**：**ふるまいの決まりは、`CLAUDE.md` に入っていないと効きません。**
`Company_Info.md` は毎回読まれるファイルではないので、そこへ書いても
「決めたつもり」になるだけです。**効かないものを「済」にしないでください。**

**②か③かは、社長の環境を見て決めます。社長に聞き直さないでください。**

> **② 社長の環境に、すでに同じことを決めた記述があるとき**
> （読者自身の `CLAUDE.md` に、保存先やファイル名の決まりが書いてある、など）
> → **そのマスは「済」。**メモ欄に「**社長の環境の決まりを使う**」と書きます。
> 「入れない」は「もう自分のがある」という意味だったので、それで正しい状態です。
>
> ```
> 保存先の決まりは、すでにお使いのものがありましたので、そちらに従います。
> ```

> **③ どこにも見当たらないまま「入れない」と言われたとき**
> → **そのマスは「保留」。**理由を1行書いて、**先へ進みます。**
>
> ```
> 保存先の決まりは、今回は入れないことにしました。
> このため、これから作るファイルの置き場所は、そのつどお尋ねします。
> あとから決めていただくこともできます。
> ```

> **① 会社の情報のとき**
>
> ```
> 会社の情報は、Shared_Assets の中に記録しました。
> 就業規則（CLAUDE.md）のほうは、お入れにならないと決められた部分なので、
> そちらには書いていません。あとから入れることもできます。
> ```

**どの場合も、止まらないでください。黙って書き込むこともしないでください。**

### B　そのまま置くもの（先に済ませます）

**いま開いているフォルダに、次の3つがあるかを実際に見てください。**
**ないものだけ、`AI_Office` の中から、いま開いているフォルダの直下へ移動してください。**

| 移動するもの | 何のためか | すでにあるとき |
|---|---|---|
| `Shared_Assets/` | 会社情報・資料の作り方・文章の決まり・請求書などのテンプレートを入れる場所 | **触らない。**社長のものが正です |
| `Programs/` | 作ったプログラムの置き場 | 同上 |
| `.gitignore` | **鍵になるファイルを、変更履歴に記録させないための一覧** | **中身を比較して、足りない行だけを足す**（消さない） |

**`.gitignore` だけは、有っても中を見てください。** これが足りないと、
**接続の鍵が変更履歴に記録されます。いちど記録されると、消しても履歴に残ります。**

移動したら、社長に1行だけ伝えます。

```
会社の情報やロゴを入れておくフォルダ（Shared_Assets）を、
「AI_Office」フォルダから、これまで Claude Code で使ってきたフォルダの中へ移動しました。
このフォルダに入れる会社の情報やロゴは、このあと一緒に用意していきます。
```

### A　比較する手順

次の4か所を、`AI_Office` の中と、いま開いているフォルダとで比較します。

| 比較する場所 | 見るもの |
|---|---|
| `CLAUDE.md` | 節ごとに、社長の環境にない節はどれか |
| `.claude/settings.json` | 禁止事項の1行ごとに、社長の環境にない行はどれか |
| `.claude/rules/` の3ファイル | 決まりの単位で、社長の環境にないものはどれか |
| `.claude/skills/` の9フォルダ | 事務用品の単位で、社長の環境にないものはどれか |

**中身を全部並べないでください。** 社長が読むのは、違いだけです。

違いは `SETUP_STATUS.md` の「このキットにあって、手元にないもの」の表に、
**1件ずつ**書き出します。この表を使うために、`AI_Office/SETUP_STATUS.md` を
いま開いているフォルダへ**1つだけ**写してください（この1つは、写して構いません）。

書き方は、1件につき3つです。

| 何が違うか | 適用すると何が変わるか | 社長の判断 |
|---|---|---|

**「適用すると何が変わるか」は、業務の言葉で書いてください。**
「rules に○○を追加」ではなく「報告に、まだ終わっていないことが必ず書かれるようになります」。

### 決めるのは社長です

```
お手元の環境と、キットの中身を比較しました。
キットにはあって、お手元の環境にはないものが〈N〉件あります。

入れると何が変わるのかを、1件ずつご説明します。
入れるか入れないかは、ひとつずつ決めてください。全部入れなくても構いません。
```

**勝手に入れないでください。1件ずつ承認を取ります。**
社長が「要らない」と言ったものは、それで終わりです。**理由を聞き返さないでください。**
断られたことも、表の「社長の判断」の欄に残します。**黙って消さないでください。**

### 比較が終わったら、そのまま開設へ進む

**ここで `AI_Office` を消させないでください（2026-09-15に修正）。**
**開設の手順（STEP 1・STEP 2）は、そのフォルダの中にあります。**
先に消すと、社長は続きができなくなります。

```
キットとの比較が終わりました。さらに、AIオフィスの開設作業を続けます。
「AI_Office」フォルダは、まだ削除しないでください。
この先の手順が書かれたファイルが、「AI_Office」フォルダの中に入っています。
```

**そのまま STEP 1 へ進んでください。** 開設作業が終わったあと（18項目がすべて「済」か「保留」になったあと）、
**STEP 3 の最後で**片づけを伝えます。

```
（開設作業が終わってから、office-setup の STEP 8 の締めくくりの文に続けて言う。17と18を見送った場合は、16のあと）
「AI_Office」フォルダは、もう使いません。
お手数ですが、社長ご自身で「AI_Office」フォルダを削除してください
（私には、ファイルを削除する操作が禁止されている場合があるためです）。

キットの新しいバージョンがリリースされると、ニュースレターでお知らせがあります。
お知らせを受け取ったら、私に「オフィス開設キットの更新を見て」とおっしゃってください。
変わった点をご説明し、入れるかどうかを1件ずつ決めていただきます。
```

### この場面で、やってはいけないこと

- **`AI_Office` フォルダで Claude Code を開き直させること。**
  そちらには社長の仕事のファイルが1つもありません。AIオフィスが2つになります
- **社長の `CLAUDE.md`・`settings.json` を、キットのもので置き換えること**
- **違いを一括で入れること**

---

## このファイルに書かれていないことを聞かれたら

キットの中の該当するファイルを読んで答えてください。それでも分からなければ、
**分からないと言ってください。** 推測で答えないでください。
