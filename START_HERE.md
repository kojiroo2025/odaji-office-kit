# START_HERE　オフィス開設の進め方

**このファイルは、社長ではなくAI従業員（あなた）に宛てたものです。**
社長から「このキットを私の環境に入れてください」と言われたら、ここを読んで、
そのとおりに進めてください。

---

## 大原則（これを外すと、あとの手順は全部無意味になります）

**① 技術の説明を並べないでください。**
社長は経営のことで頭がいっぱいです。設定項目を1つずつ説明して「はい」と
答えてもらう進め方は**採りません**。判断材料を持たない相手に説明しても、
判断はできません。しかも、説明を理解できるのは自分で全部作った人だけで、
**このキットをいちばん必要としている人とは逆**です。

**② 判断を求めるのは、作業のあと、業務上の結果として、原則1問だけです。**

**③ 報告は、業務の言葉で3点だけ。読む量は15行・3分を上限の目安にします。**

**④ 理解は、説明からではなく、実際に1回動かすところから来ます。**
だから STEP 0 の最後にある「禁止事項が実際に効いているかを試す」を飛ばさないでください。
**書いただけの設定は、効いていません。** 止まるところを社長に見せることが、いちばん大事です。

---

## STEP 0　設置する（社長に何も聞かずに進めてよい）

**設置の途中で、社長の画面に「よろしいですか」という確認が何度か出ます。**
`.claude` の中は守られた場所なので、あなたが書き込むときに Claude Code 本体が
社長に確認します（社長が確認の画面で「このセッションのあいだは許可する」を選ぶと、そのセッションでは以後、確認は出ません）。**これは正常です。** 先にそう伝えてから始めてください。

展開されたキットのフォルダから、デジタルオフィスへ次のとおり置きます。

| キットの中 | 置き先 |
|---|---|
| `CLAUDE.md.template` | `CLAUDE.md`（**注記は取り除く**） |
| `.gitignore.template` | `.gitignore`（**注記は取り除く**） |
| `.claude/settings-template.jsonc` | `.claude/settings.json`（**行頭が `//` の行を取り除いて有効なJSONにする**） |
| `.claude/rules/` の3ファイル | `.claude/rules/` |
| `.claude/skills/` の9フォルダ | `.claude/skills/` |
| `Shared_Assets/` 一式 | `Shared_Assets/` |
| `Programs/README.md` | `Programs/` |
| `SETUP_STATUS.md` | `SETUP_STATUS.md` |
| `VERSION` の内容 | `.claude/OFFICE_KIT_VERSION`（更新確認に使います） |

`README.md`・`CHANGELOG.md`・`START_HERE.md`・`LICENSE` は**置き先がありません。**
ダウンロードしたフォルダにそのまま残してください。

置き終わったら、**`CLAUDE.md` の「デジタルオフィスの場所」に、いま開いているフォルダを
書いてください。** ここは社長に聞かなくても分かる唯一の項目です（`SETUP_STATUS.md` の②）。

### すでに同じ名前のファイルがあるとき

**上書きしないでください。** 社長がすでに育てたものが正です。

- `CLAUDE.md`・`.gitignore`・`settings.json` → **足りない節・行だけを足す**
- `.claude/rules/`・`Shared_Assets/` の同名ファイル → **触らない。** 内容の違いは
  STEP 3 で一覧にして、社長に選んでもらいます

### 置き終わったら、1つだけ確かめる

禁止事項が実際に効いているかを、その場で試してください。
**書いただけで効いていない設定は、効いているつもりでいる分だけ、無いより危険です。**

禁止事項（`settings.json`）は、書き換えるとその場で反映されます。
実際に禁止した操作を打って、拒否されるところを社長に見せてください。
安全に試せる方法が無ければ、実行せずに理由を説明してください。

**試すときの注意が2つあります。**

**①　試したファイルは、あなたには消せません。** いま削除を禁止したからです。
**それが正しい状態です。** 消せなかったことを失敗と受け取らないでください。
作ったファイルの名前を社長に伝えて、あとで手で消してもらってください。

**②　すでに Claude Code を使っている環境では、切り分けが要ります。**
社長がこれまでに作った禁止設定があると、キットの金庫が効いたのか、
前からあった設定が止めたのかが分かりません。**この金庫だけが止める形**を選んで
試してください（前からある設定に当たらない書き方で打つ、という意味です）。
分からなければ、社長に「前から入れている禁止設定はありますか」と聞いてください。

**③　設置そのものが拒否されたときは、迂回路を探さないでください。**
会社によっては、設定ファイルへの書き込みを組織の側で止めていることがあります。
そのときは、**出来上がりのファイルを別の場所に用意し、コピーする命令を1行だけ
社長に渡してください。** あなたが別の方法で書き込もうとすると、止めている理由ごと
迂回することになります。

## STEP 1　報告して、1問だけ聞く

**業務の言葉で3点だけ**報告します。技術用語を使わないでください。

```
デジタルオフィスの設定を入れ終わりました。

変わったこと：〈例：資料の書式と文章の書き方が、全員で統一されました〉
できなくなったこと：〈例：ファイルの削除を、私が実行できなくなりました〉
うまくいかないとき：〈例：「元に戻して」とおっしゃれば、入れる前の状態に戻せます〉
```

その直後に、次の一言を**1回だけ**伝えてください。ここから社長への質問が始まるからです。
この一言は、あとの STEP では繰り返しません。

```
これから、いくつか質問をします。
私の質問の意味が分からないときは、遠慮なく「どういう意味？」と聞いてください。
言い方を変えて、もう一度お聞きします。
```

質問は、`.claude/rules/work-style.md` の「社長に質問するときの書き方」を読んで、
そのとおりに書いてください（何について聞くのか・答えを何に使うのか・どう答えればよいのか）。

そのうえで、**業務上の結果として1問だけ**聞きます。

```
削除の操作を、私が実行できない設定にしました。
今後、ファイルの整理には社長の手が要ります。
（1）このままにする　（2）私が消せるようにする
```

聞くのはここだけです。ほかの設定について意見を求めないでください。

### そして、いったん閉じて開き直してもらう　←　ここを飛ばさない

**いま置いた `CLAUDE.md` と `.claude/rules/` は、このセッションでは効きません。**
これらは Claude Code を開いたときに1回だけ読み込まれるので、**あとから置いても、
いま動いているセッションには反映されません**（禁止事項とスキルは、その場で反映されます）。

このまま続けると、**就業規則もルールも効いていない状態で会社の情報を入れることになります。**
そこで、社長にこうお伝えしてください。

**〈　〉には、いま開いているフォルダの場所を、あなたが書き入れてください。**
社長に聞く必要はありません。**ここを空欄のまま渡さないでください。**

```
ここでいったん Claude Code を閉じて、開き直してください。
いま入れた就業規則とルールは、次に開いたときから効きます。

開き直すときは、必ず〈 いま開いているフォルダ 〉を開いてください。
別のフォルダで開くと、いま入れたものが1つも読み込まれません。

開き直したら「続きをお願いします」とだけおっしゃってください。
会社の情報を入れるところから再開します。

（確かめたい方は、開き直したあとに /context と打つと、
　就業規則とルールが読み込まれたことが見られます）
```

**社長が開き直すまで、先へ進まないでください。**

**開き直したあと、最初にフォルダを確かめてください。** 社長が別のフォルダで
開いてしまうことがあります。`CLAUDE.md` と `SETUP_STATUS.md` が見えなければ、
**そこは違うフォルダです。** 作業を続けず、正しいフォルダで開き直すよう伝えてください。
（この確認が要るのは、実際に起きたためです。別のフォルダで開くと、
就業規則もルールも読み込まれないまま STEP 2 に入ってしまいます）

---

## STEP 2　会社のことを、最小限だけ埋める

**ここからは、開き直したあとです。** 社長が「続きをお願いします」とおっしゃったら、
このファイルの STEP 2 から再開してください。

**この段階では、全部を埋めようとしないでください。** 社長に聞くのは、次の3つだけです。
残りは、`SETUP_STATUS.md` の「続きの回」と「使う場面が来たら」に回します。

（`CLAUDE.md` の「デジタルオフィスの場所」は、STEP 0 で置いたときに埋めてあるはずです。
まだなら、いま開いているフォルダをそのまま書いてください。社長に聞く必要はありません。）

### 1. 会社の情報を、ホームページか名刺から読み取る

```
書類やメールに、会社のことを正しく書けるようにしておきます。
会社のホームページか、社長のプロフィールが載っているページのURLを教えてください。
無ければ、名刺を1枚、写真に撮って見せていただくのでも大丈夫です。
```

読み取った内容で `Shared_Assets/Company_Info.md` と `CLAUDE.md` の会社情報を埋め、
社長に確かめてもらいます。**あなたの読み違いを防ぐためです。**

```
次のように読み取りました。違っているところがあれば、そこだけ教えてください。
すべて合っていれば「はい」とだけおっしゃってください。

〈会社名／代表者／住所／電話／メール〉
```

それでも埋まらない項目は、**「未定」と書いて残します。**
**推測で埋めないでください。** 「未定」の項目は、使う場面が来たときに聞きます。

### 2. ファイルの保存場所を、案を見せて決める

いまのデジタルオフィスのフォルダを見て、`CLAUDE.md` の「保存先のルール」の案を作り、
次のように聞きます。

```
これから作るファイルの保存場所を、次のように決めたいと思います。
いまお使いのフォルダを見て、案を作りました。

  作業中のもの　　　→ 〈作業中〉
  完成した書類　　　→ 〈成果物〉
  お客様ごとの資料　→ 〈顧客〉の中に、お客様ごとのフォルダ

いまあるファイルは動かしません。
これでよろしいですか。変えたいところがあれば、そこだけおっしゃってください。
```

**社長が「はい」と言うまで、フォルダを作ったり、ファイルを動かしたりしないでください。**

### 3. 会社の顔（ロゴと色）を聞く

```
会社のロゴと、会社の色を教えてください。
資料やメールを作るとき、これがあると「あなたの会社のもの」になります。

・ロゴ：画像ファイルがあれば、この会話に入れてください
・色　：会社の色が決まっていれば、色の名前でも、色の番号でも構いません
　　　　（例：濃い赤、#C62828）
```

**まだ決まっていない、ファイルが手元にない、というのは、よくあることです。**
そのときは、こう伝えて**次へ進んでください。**

```
承知しました。準備ができたときに共有してください。
それまでは、色を使わない形で作ります。
```

**保留にしたことと理由を、`SETUP_STATUS.md` の「保留にしたものと、その理由」に
1行書いてください。** 書かない保留は、忘れられた保留と同じです。

もらったものは、こう置きます。

- ロゴの画像 → `Shared_Assets/` の直下（ファイル名はそのままで構いません）
- 色 → `Shared_Assets/Design_Rules.md` の「色」の欄

**ここで聞くのは、ロゴと色だけです。** 文字の大きさ・見出しの付け方・余白といった
**資料の作り方は、まだ決めません。** 何を作るかが決まってからでないと、正解が決まらないためです
（`SETUP_STATUS.md` の「使う場面が来たら」に入っています）。

## STEP 3　進捗の表を数え直して、今日を終える

`SETUP_STATUS.md` は、**1マス終わるごとにその場で書き換えてきたはず**です
（そう書いてあります）。ここでやるのは、書き忘れが無いかを確かめて、数え直すことです。

**実際に確かめてから記入してください。** 推測で「済」にしないでください。
ファイルの内容を開いて見てください。

そのうえで、社長にこう伝えます。

```
今日はここまでです。オフィスが整いました。

会社の情報が入り、危ない操作は止まるようになり、消したものは元に戻せます。
文章の書き方も入っています。**AI従業員を、いつ雇ってもよい状態です。**

残っているのは〈N〉件です。急ぎません。続きをやりたくなったら、
「オフィス開設の続きをやりましょう」とおっしゃってください。
何回に分けても構いません。
```

**ここで止まってください。** AI従業員を作りましょう、と自分から言い出さないでください。
**このキットの役目は、オフィスを整えることまでです。**
1人目を作るのは、ニュースレターの実践編（次の号から）です。

社長から「AI従業員を作りたい」と言われたときだけ、`.claude/skills/skill-writing/` を
読んで進めてください。**道具は渡してあります。順番を決めるのは社長です。**

〈N〉は、`SETUP_STATUS.md` の「開設の日」と「続きの回」にある「未」の数です。
「使う場面が来たら」のマスは数えません。

**残りを今日中に片付けようとしないでください。** この作業でいちばん時間が
かかるのは、書くことではなく決めることです。

続きは `.claude/skills/office-setup/` が担当します。

---

## すでにClaude Codeを使い込んでいる環境のとき

**別の手順は用意していません。上のSTEP 0から同じように進めてください。**

STEP 0 で「すでに同じ名前のファイルがある」ものが多くなり、STEP 3 で作る表は
最初から多くが「済」になります。それでよいのです。

そのとき、STEP 3 の報告に**もう1つの一覧**を添えてください。

> **このキットにあって、お手元に無いもの**

`.claude/rules/` の3ファイルと、キットのスキルに書かれているルールを、
社長の環境の同じ場所と読み比べます。違いがあれば、**1件ずつ「何が違うか」
「入れると何が変わるか」**を書いて示します。

**勝手に当てないでください。1件ずつ社長の承認を取ります。**
社長が「要らない」と言ったものは、それで終わりです。理由を聞き返さないでください。

---

## このファイルに書かれていないことを聞かれたら

キットの中の該当するファイルを読んで答えてください。それでも分からなければ、
**分からないと言ってください。** 推測で答えないでください。
